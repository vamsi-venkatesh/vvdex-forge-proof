# Capability measurements

Forge groups existing certified exam measurements by capability. The index in
[`capabilities/index.json`](capabilities/index.json) is a public projection of
verified sealed records. It is generated by the same publication as the website;
it is not a second evaluator or a hand-maintained score database.

## Taxonomy

| Group | Published exam scope |
| --- | --- |
| Software Engineering | xmltodict issue 257; historical toolz issue 602 and parse issue 102 |
| Browser / Computer Use | Order desk browser workflow |
| RAG / Grounding | Grounded question answering; the measured revision is retired |
| Memory | Cross-session fact update; the measured revision is retired |
| Agent Reliability | Tool-loop fault recovery |
| Multimodal Annotation | Image, sampled-video, text and structured-data tasks; native video and audio remain unmeasured |

This describes published evidence, not the full internal exam inventory or a
claim of general model competence. Exam certification establishes the evaluation
instrument's checks; it is separate from the model's performance on that exam.

## Counting and conditions

An attempt is identified by its sealed record digest and rollout identity. The
same attempt may appear in more than one campaign publication. Those references
remain available, but the attempt is counted once. The five-exam campaign includes
the exact records of the two smaller software/knowledge and browser/recovery
publications. The historical one-attempt cells remain separate from later runs.

A measurement retains its exam fingerprint, lane configuration commitment,
condition cohort and campaign references. There is no category composite or
universal model score. Certified passes divided by graded attempts is shown only
for an individual measurement; graded means certified passes plus attributable
model failures. Lane errors, withheld and invalid outcomes are separate. Rates
are suppressed below ten graded attempts; the underlying counts stay visible.
Repeated attempts on one fixed task do not establish broad capability coverage.

Comparison eligibility checks the recorded exam fingerprint, runtime, limits,
engine and input delivery. Missing information does not establish equivalence.
Historical records can have measurable outcomes while comparison conditions
remain unproven. A difference in repository cleanliness is disclosed without
claiming that execution code necessarily changed. Per-task Annotation coverage
may account for multiple condition cohorts; it is not a pooled comparison rate.

## Input delivery and telemetry

Image delivery, sampled video frames, native video, native audio, text and
structured data have separate identities. Historical delivery is derived only
from sealed receipts or a sufficient sealed contract, never from the exam's
modality label. The existing robot-video result concerns sampled frames. It says
nothing about native-video performance. Text and structured outcomes remain
measured where historical delivery details are unproven.

Elapsed time, steps, tokens and cost are shown only when recorded. Medians use
individual graded-attempt observations, with observed/eligible coverage. Missing
values are not zero. Costs are not reconstructed from current prices. Approved
quality metrics remain specific to their annotation task.

## Following the evidence

Each measurement points to campaign summaries, sealed record commitments and
attempt identities with certified outcomes. Public record projections also state the effective certified outcome after canonical harness review; raw sealed verdict fields remain distinguishable. The exact canonical record bodies,
private media details, rubrics, answer keys and graders are withheld. Public
projections do not hash to the canonical record commitment. A holder of the
sealed canonical bytes can independently recompute that commitment.

Run `./verify.sh` to check the package manifest, agreement of record commitments,
Annotation certification joins and capability counts/reference integrity against the per-attempt public record projections, including complete lane inventories across condition cohorts. This
checks the public artifacts' consistency; it does not independently re-run a
withheld grader or prove unrecorded experimental conditions.
