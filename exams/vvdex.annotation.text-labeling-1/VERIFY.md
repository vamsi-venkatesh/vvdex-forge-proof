# Verify vvdex.annotation.text-labeling-1

This is certification evidence, not a model campaign or a runnable exam.

Join exam.public.json, certification-receipt.json and report.public.json on examFingerprint. Hash the receipt bytes with SHA-256; compare with report.public.json certificationReceiptSha256. digests.json records each adjacent public file's SHA-256; verify those bytes independently. The certificationEvidenceDigest identifies the private sealed bundle; only a holder can audit its contents. Public file hashes establish consistency, not independent reproduction of the private grader.

No gold, hidden grading logic, raw submission, exact rubric or adversarial payload is distributed.
